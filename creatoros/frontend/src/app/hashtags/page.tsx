"use client";

import TabbedModule from "@/components/TabbedModule";

export default function HashtagsPage() {
  return (
    <TabbedModule
      featureKey="hashtags"
      title="Hashtags"
      subtitle="Search hashtags, inspect tag info, browse recent and top posts, and measure tag analytics."
    />
  );
}