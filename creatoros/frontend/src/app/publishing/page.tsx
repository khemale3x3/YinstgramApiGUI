"use client";

import TabbedModule from "@/components/TabbedModule";

export default function PublishingPage() {
  return (
    <TabbedModule
      featureKey="publishing"
      title="Publishing"
      subtitle="Upload photos, videos, reels, stories, albums and IGTV. Off by default."
    />
  );
}