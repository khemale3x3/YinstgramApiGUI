"use client";

import TabbedModule from "@/components/TabbedModule";

export default function LocationsPage() {
  return (
    <TabbedModule
      featureKey="locations"
      title="Locations"
      subtitle="Search locations, view location details, media and discovery feeds."
    />
  );
}