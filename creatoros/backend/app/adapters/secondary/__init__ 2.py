"""Secondary (driven) adapters.

These implement the ports from `app.core.ports`. They may depend on
instagrapi, the filesystem, or any external system — but nothing depends on
them.
"""
from __future__ import annotations
