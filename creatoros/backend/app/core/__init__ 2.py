"""Core — the application's hexagonal core.

Domain entities, ports (interfaces) and use-case services live here. Nothing
in `core` imports from `app.adapters`, instagrapi or FastAPI.
"""
from __future__ import annotations
