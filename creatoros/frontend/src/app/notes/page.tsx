"use client";

import TabbedModule from "@/components/TabbedModule";

export default function NotesPage() {
  return (
    <TabbedModule
      featureKey="notes"
      title="Notes"
      subtitle="Personal notes, notes feed and note management."
    />
  );
}